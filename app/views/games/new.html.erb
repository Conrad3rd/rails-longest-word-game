<div class="text-justify"></div>
  <h1>New Game</h1>
  <p>
    <% @new_word.each do | word | %>
      <button type="button" class="btn btn-primary btn-square-lg"><%= word.upcase %></button>
    <% end %>
  </p>
  <p>What is the longest word you can find?</p>
    <form class="" action="/score" method="POST" data-turbo="false">
      <div class="">
        <input class="" type="text" name="new" placeholder="type in your word">
        <%# <input type="submit" value="new_game"> %>
        <button type="submit" value="new_game" class="btn btn-primary mb-2">Play</button>
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      </div>
    </form>
</div>
